{
  "name": "gatsby-starter-buzzing",
  "private": true,
  "scripts": {
    "develop": "npm -w example run develop",
    "start": "npm -w example run start",
    "start:prod": "LOCAL=false yarn start",
    "build": "npm -w example gatsby build",
    "build:dev": "LOCAL=true npm -w example gatsby build",
    "clean": "npm -w example gatsby clean",
    "serve": "npm -w example gatsby serve",
    "release": "release-it"
  },
  "license": "0BSD",
  "workspaces": [
    "gatsby-theme-buzzing",
    "example"
  ],
  "dependencies": {
    "gatsby-theme-timeline": "^1.0.4",
    "release-it": "^14.2.2"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^2.0.0",
    "release-it-yarn-workspaces": "^2.0.0"
  },
  "version": "1.0.1"
}
