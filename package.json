{
  "name": "gatsby-starter-buzzing",
  "private": true,
  "scripts": {
    "develop": "npm -w example run develop",
    "start": "npm -w example run start",
    "start:prod": "LOCAL=false npm -w example run start",
    "build": "npm -w example run build",
    "build:dev": "LOCAL=true npm -w example run build",
    "archive:dev": "npm -w example run archive:dev",
    "archive": "npm -w example run archive",
    "archive:index": "npm -w example run archive:index",
    "archive:detail": "npm -w example run archive:detail",
    "clean": "npm -w example run clean",
    "serve": "npm -w example run serve",
    "release": "release-it"
  },
  "license": "0BSD",
  "workspaces": [
    "gatsby-theme-buzzing",
    "example"
  ],
  "dependencies": {
    "release-it": "^14.2.2"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^2.0.0",
    "release-it-yarn-workspaces": "^2.0.0"
  },
  "version": "1.8.0"
}
